<?php
  $power = pow(2, 7);
  $random = rand(); // you can limit this between 2 numbers by providing 2 parameters e.g rand(1, 1000) will generate a number between 1 & 1000
  $squared_root = sqrt(49); // Square Root
  $round_up = ceil(5.8);
  $round_down = floor(5.4);
  $round_classic = round(5.5);

  // https://php.net/manual/en/ref.math.php
?>